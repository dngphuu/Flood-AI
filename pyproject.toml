[project]
name = "flood-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.5",
]

[tool.uv.workspace]
members = [
    "ai_service",
    "backend_service",
]
